# 道玄文集 6.0 → 6.1 改动说明

> 输出目录：`F:\WorkBuddy\wx`
> 原始目录：`F:\WorkBuddy\w`（原始文件不动，供参考）

---

## ✅ 已实施的修改（共 8 项）

### 1. 封面删除空友链
**文件：** `index.html`
- 删除了 5 个空白占位友链（`#`、`#`、`#`、`#`、`#`）
- 保留了 5 个有效链接：微信公众号、三省轩主文集、道玄 TV 软件、古诗文网、中华书库

---

### 2. jianjie.html 修复错误
**文件：** `jianjie.html`
- 删除了破损的 `</p` 标签（悬垂标签）
- 删除了多余的 `</p></p>` 层叠标签
- 两段版权文字合并为一个语义完整的 `<p>`
- 整体结构重写为：关于作者（两段介绍文）+ 关于版权（一段声明）

---

### 3. 移除微信分享卡片（全站，2026-07-28）
**文件：** `index.html` · `index1.html` · `jianjie.html` · `search.html`；删除 `og-cover.jpg` / `生成og封面.py` / `_headers`
- 删除全部 Open Graph / Twitter Card 标签（微信链接卡片无实际意义，改为直接发链接）
- `og:image` 不再使用，`wsf.jpg` 等旧配图保留为未使用资源

---

### 4. 封面本地化背景图
**文件：** `cover.css`
- 原 ZCOOL 图片（`img.zcool.cn`）有防盗链，直接引用会 403
- 改为 CSS 渐变（仿宣纸+水墨晕染感），不依赖任何外部图片，0 加载失败
- 添加注释说明：如需图片背景，取消一行注释并放入 `cover-bg.jpg` 即可

> 💡 若将来想用回原图，下载 `https://img.zcool.cn/community/01f3fa5d18d473a801213f26e4776a.jpg@1280w_1l_2o_100sh.jpg`，保存为 `F:\WorkBuddy\wx\cover-bg.jpg`，取消 cover.css 中的注释即可。

---

### 5. 翻书动画遮罩修复
**文件：** `index.html` · `cover.css`
- 问题：`rotateY(-150deg)` 时露出空白背面（翻转过程的杂色）
- 方案：在 `.book-cover` 前插入 `.book-back` 层（书页横截面渐变质感）
- 翻页时 `.book-back` 立即覆盖原位，消除空白闪动

---

### 6. 分卷加载文章目录
**文件：** `index1.html`
- 当文章超过 50 篇时，自动在目录页顶部生成「卷 1 / 卷 2」按钮
- 每卷 50 篇，用户按卷阅读，避免目录过长
- 卷切换后同步重置分类筛选和搜索

> ℹ️ 当前共 105 篇，将显示「卷 1 · 50 篇」+「卷 2 · 50 篇」+「卷 3 · 5 篇」

---

### 7. Service Worker 版本升级
**文件：** `sw.js`
- 缓存版本号 `dxwj-v2` → `dxwj-v3`
- 浏览器自动清除旧缓存，下载新文件
- 确保线上更新后读者立即看到最新内容

---

### 8. 百度统计异步加载
**文件：** `index.html` · `index1.html`
- 统计脚本改为 `<script async>`，不阻塞页面渲染
- 页面加载速度不受影响

---

## ⏭️ 未实施的建议（可后续处理）

| 建议 | 原因 |
|------|------|
| RSS 订阅 | 古典文集类网站 RSS 使用率低，维护成本高 |
| 全键盘导航 | 当前 ← → 翻篇键已存在，完整键盘快捷键非核心需求 |
| GitHub Pages 内置搜索 | GitHub Pages 原生不支持，改用第三方方案（Jekyll 插件等）增加复杂度 |
| 字典链接跨域跳转修正（github.io ↔ pages.dev） | `dict.js`、`dict.html`、`index1.html` 导航链接均硬编码 `dxwj.pages.dev`；在 github.io (jygldj.github.io/w/) 使用字典后返回键跳到 pages.dev 而非 github.io。暂不修正——github.io 和 pages.dev 内容一致，实际影响人数极少（主要是维护者预演用），修正需在多处增加域名判断逻辑，得不偿失。 |

---

## 🚀 上线步骤（6.0 → 6.1 迁站记录，已废弃）

<!-- ⚠️ 以下步骤已废弃，仅保留供历史参考。当前日常流程见 README.md 第五节。 -->

~~1. 把 `F:\WorkBuddy\wx` 里的**全部文件**复制到 GitHub 仓库根目录（覆盖同名文件）~~
~~2. GitHub Desktop → Commit to master → Push origin~~
~~3. 约 1 分钟后访问 `https://jygldj.github.io/w/` 验证~~

<!-- > ⚠️ 注意：`articles.js` 是核心数据文件，如需更新文章，请用 `build.html` 更新工具重新生成后提交。 -->

**当前实际流程（请以 README.md 第五节为准）：**
双击 `F:\github-dx\wx\更新网站.bat`（或 `push-now.bat`）→ 写稿 → GitHub Desktop Commit & Push → `dxwj.pages.dev` 自动部署。

---

## 🧹 过渡项目清理记录（2026-07-28）

### 9. 删除 Cloudflare Pages 项目 `wx` (wx-5i8.pages.dev)
- **删除原因**：与正式站点 `dxwj.pages.dev` 共用 GitHub 仓库（`jygldj/wx`），每次 push 触发双部署，浪费 Cloudflare 构建配额。
- **删除前验证**：
  - `https://dxwj.pages.dev/` 划词查字典、阅读、搜索功能全部正常
  - `wx-5i8.pages.dev` 与 `dxwj.pages.dev` 显示内容一致（同一仓库）
  - 无外部链接指向 `wx-5i8.pages.dev`
- **删除后影响**：✅ 零。dxwj 站点运行无任何变化。
- **保留策略**：GitHub 仓库 `jygldj/wx` 不动；只删 Cloudflare Pages 项目。

### 10. 删除 Cloudflare Worker `dict-worker` (dict-worker.gswsf.workers.dev)
- **存在背景**：早期划词字典方案使用独立 Worker 部署，域名 `*.workers.dev` 在国内被运营商封锁，2026-07-25 ~ 2026-07-28 切换为 Pages Functions 同域方案 (`dxwj.pages.dev/api/dict`)。
- **删除前状态**：
  - 仍绑定 KV 命名空间 `DICT_KV`（id `e9e3ca2874cd4affbc778f7b7e26f765`）
  - 实际访问入口 `https://dict-worker.gswsf.workers.dev/` 国内打不开
  - 浏览器划词查字典全部走 `dxwj.pages.dev`，dict-worker 0 真实流量
- **删除流程**：
  1. 在 Worker 绑定页解除 `DICT_KV` 绑定（**2026-07-28 21:00 已执行**）
  2. 对 `https://dxwj.pages.dev/` 反复 Ctrl+F5 刷新验证 → 无异常
  3. 删除 Worker 项目（**2026-07-28 21:00 已执行**）
- **删除后影响**：✅ 零。键盘快捷键、划词查字典、字典独立页全部正常。
- **架构定型**：✅ 站点 `dxwj.pages.dev` + Pages Function `/api/dict` + KV `DICT_KV` 三个组件成为唯一链路。

---

## 📝 旧域名索引（已废弃，仅作历史参考）

| 旧域名 | 替代为 | 废弃时间 |
|---|---|---|
| `daoxuanwenji.pages.dev` | `dxwj.pages.dev` | 2026-07-25 |
| `wx-5i8.pages.dev` | `dxwj.pages.dev` | 2026-07-28 |
| `dict-worker.gswsf.workers.dev` | `dxwj.pages.dev/api/dict` | 2026-07-28 |
| `jygldj.github.io/w/` | `dxwj.pages.dev` | 2026-07-24 |
