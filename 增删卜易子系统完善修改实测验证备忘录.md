# 增删卜易子系统 · 完善修改实测验证备忘录

> 整理日期：2026-08-08
> 适用：`jygldj/wx` 仓库 `zsby/` 子系统
> 说明：本备忘录记录本次完善修改（5 个 PR，均已合入 main）的全部改动点与逐项实测验证方法。**实测时先部署最新 main（Cloudflare Pages 自动重新部署），再按 §二 逐项操作。**
> 线上地址：`https://dxwj.pages.dev/`（以实际部署域名为准）

---

## 一、本次修改总览

| PR | 分支 | 类别 | 改动摘要 |
|---|---|---|---|
| #1 | `260808-fix-bug1-jiegua-comma` | BUG | 修复释卦页 `inferQuestionType` 数组缺逗号致命 Bug |
| #2 | `260808-fix-sec-history-xss` | 安全 | 历史页 XSS 转义；/api/ai 来源白名单 + 可选令牌 |
| #3 | `260808-fix-log-jiegua-history` | 逻辑 | 过时提示文案修正；历史记录去重防膨胀 |
| #4 | `260808-refactor-arc1-deadcode` | 架构 | 删除死代码；README 文档漂移修正 |
| #5 | `260808-refactor-arc2345-converge` | 架构 | ARC-2~5 收敛（旬空/飞伏/六神/用神映射单源化）+ 算法层单测 |

**验收基线**：算法层单测 17/17 全绿（`cd zsby/ly && node --test`），全部改动已通过回归验证。

---

## 二、实测验证清单（逐项操作）

### 2.1 BUG-1：释卦页关键词映射失效（PR #1）

**背景**：`jiegua.html` 数组字面量 `['子','子孙']['孙','子孙','孩子','子孙']` 缺逗号，被解析为下标访问得到 `undefined`。后果：①"子/孙/孩子"关键词匹配失效；②问题不含任何已有关键词时 `inferQuestionType` 抛 TypeError，rw7/rw8 计算被 try-catch 吞掉，用神/忌神/仇神/旺衰评分静默失效。

**实测步骤**：

1. 打开释卦页，输入问题 **"我该不该跳槽"**（不含任何关键词），完成排盘。
   - ✅ 期望：页面正常显示，控制台**无** `Cannot read properties of undefined (reading '0')` 报错。
   - ✅ 期望：用神/忌神/仇神/旺衰评分正常展示，不再显示"未计算"。
   - ✅ 期望：`xuanYongShen` 返回 `reason:'未知所问之事，无法定用神'`，AI 提示词提示按规范词语重问。

2. 输入问题 **"我的孩子今年运势如何"**（含"孩子"）。
   - ✅ 期望：用神正确映射为 **子孙**。

3. 输入问题 **"我想算算婚姻"**（含"婚"）。
   - ✅ 期望：用神正确映射为 **官鬼**。

4. 逐项输入含以下关键词的问题，确认映射均生效：
   - 财运/求财/生意 → 妻财；事业/升职/官 → 官鬼；学业/考试 → 父母；病/健康 → 官鬼；出行/旅行 → 父母；失物/丢 → 妻财；找/寻 → 子孙。

---

### 2.2 SEC-1：历史页 XSS 转义（PR #2）

**背景**：历史页用户可控字段（姓名/所问/时间等）直接拼接 `innerHTML`，含 `<`/`>`/`&` 的输入会执行脚本。现已全字段 `escapeHtml` 转义。

**实测步骤**：

1. 在起卦页填写求卦者姓名：**`<script>alert('xss')</script>`**，问题填 **`<img src=x onerror=alert(1)>`**，完成排盘（进入历史）。
2. 打开历史页。
   - ✅ 期望：上述输入以**纯文本**显示（`<script>`、`<img>` 原样可见），**无**弹窗、无脚本执行。
   - ✅ 期望：`&`、引号等特殊字符正常显示，不破坏页面结构。

3. 反向验证：正常姓名/问题（如"张三"、"问财运"）历史页显示正常。

---

### 2.3 SEC-2：/api/ai 来源白名单 + 可选令牌（PR #2）

**背景**：`functions/api/ai.js` 新增 `ALLOWED_ORIGINS` 白名单（dxwj.pages.dev / localhost / 127.0.0.1，含端口匹配）+ 可选 `AI_GATE_TOKEN` 请求头校验。仅拦截非白名单来源，正常页面不受影响。

**实测步骤**：

1. **正常来源**：从线上页面（`https://dxwj.pages.dev/`）发起释卦。
   - ✅ 期望：释卦功能正常，AI 解读正常返回。

2. **跨站来源**：打开浏览器 DevTools Console，从控制台用其他 Origin 直接 POST `/api/ai`（或用 curl 带 `Origin: https://evil.example.com`）。
   - ✅ 期望：返回 **403**，不消耗 API 额度。

3. **可选令牌（仅当 Cloudflare 控制台配置了 `AI_GATE_TOKEN` 时）**：
   - 不带 `x-ai-token` 请求头 POST → ✅ 期望 403。
   - 携带正确 `x-ai-token` → ✅ 期望正常返回解读。
   - 未配置该变量时，此项跳过（纵深防御默认关闭，白名单仍生效）。

---

### 2.4 LOG-1：释卦失败提示文案（PR #3）

**背景**：原提示"请确保 config.js 中的 API_KEY 已正确配置"已过时（密钥迁入服务端环境变量）。

**实测步骤**：

1. 临时移除/清空 Cloudflare 环境的 `QWEN_API_KEY`/`QWEN2_API_KEY`（或模拟服务端解析失败）。
2. 释卦页执行释卦。
   - ✅ 期望：提示为 **"请确认服务端已配置 QWEN_API_KEY / QWEN2_API_KEY 环境变量后重新部署"**（jiegua.html:1259）。
   - ✅ 期望：提示中**不再出现**"config.js"字样。

3. 恢复环境变量后释卦，提示消失、正常解读。

---

### 2.5 LOG-2：历史记录去重防膨胀（PR #3）

**背景**：原每次排盘无条件 push 一条历史。现按 `本卦+变卦+time`（秒级）去重，同秒重复排盘不重复追加。

**实测步骤**：

1. 打开起卦页，记录当前 `guaHistory` 条数（DevTools → Console：`JSON.parse(localStorage.getItem('guaHistory')||'[]').length`）。
2. 连续快速点击排盘 **3 次**（同一秒内、相同卦象）。
   - ✅ 期望：`guaHistory` 条数**只增加 1 条**，不重复膨胀。

3. 不同秒/不同卦象排盘 2 次。
   - ✅ 期望：条数正常各 +1。

4. 释卦后回写：历史页对应条目应被命中回写 `result`，无错位。

---

### 2.6 ARC-1：死代码删除（PR #4）

**背景**：删除 `jiegua.html` 中从未执行的 `injectInlineRw7Marks` 函数（页面未加载 zhukong.js、无 `.yao-row`，遍历必然为空）。

**实测步骤**：

1. 释卦页完成一次完整排盘+释卦。
   - ✅ 期望：功能与改前完全一致，控制台无报错。
2. DevTools 搜索 `injectInlineRw7Marks`。
   - ✅ 期望：全文件**无**该函数（已删除）。

---

### 2.7 ARC-2~5：架构收敛（PR #5，单源化）

**背景**：旬空统一走 `LunarUtil.getXunKong`（lunar.js）、六神统一走 `paiLiuShen`（suanfa.js）、飞伏关系统一走 `fuShenRelationRW8`、用神映射统一为 `QUESTION_TO_YONGSHEN`/`YUAN_SHEN_MAP` 单源。**结果应与改前完全一致**。

**实测步骤**：

1. **释卦页**：起卦（如"乾为天"）后核对：
   - 六爻角标（月破/日破/暗动/真空/假空）显示与改前一致；
   - 飞伏关系长文案（"飞生伏（吉…）"）**保持不变**；
   - 用神/忌神/仇神映射结果正确。
2. **起卦页**：手动输卦后，旬空/世应/六亲显示正确。
3. **万年历页**：选择日期，旬空、六神（青龙/朱雀/…）、纳音显示正确，与改前一致。
4. **算法回归**（本地）：`cd zsby/ly && node --test` → ✅ 期望 **17/17 全绿**。

---

### 2.8 ARC-8：README 文档修正（PR #4）

**实测步骤**：

1. 打开 `zsby/README.md`。
   - ✅ 期望：目录树**无** `ly/lunar.js`、`api/qwen2.js`（均不存在于仓库）。
   - ✅ 期望：密钥说明为"环境变量 QWEN_API_KEY/QWEN2_API_KEY（Cloudflare Pages 配置）"，**无** `config.js` 密钥描述。

---

## 三、自动化回归（本地，每次改动后必跑）

```bash
cd /tmp/opencode/wx
# 算法层单测
cd zsby/ly && node --test __tests__/suanfa.test.js
# 脚本语法校验
node --check ly/suanfa.js
node --check ly/zhukong.js
```

**期望输出**：单测 `# pass 17 / # fail 0`；各 `node --check` 无输出（通过）。

---

## 四、全链路回归（起卦 → 排盘 → 释卦 → 应期 → 择吉）

1. 起卦页（jiegou.html）：手动或自动起卦 → 排盘完成。
2. 释卦页（jiegua.html）：加载卦象 → 输入问题 → 释卦 → 检查用神/忌神/仇神/旺衰/断卦全文。
3. 历史页（history.html）：确认新记录入列、释卦结果回写。
4. 万年历页（wnl.html）：确认日历渲染、旬空/六神/纳音/择吉正常。
5. 指南页（help.html）：确认导航可用。

> 任一步异常：记录现象 → 在 git 中定位对应 PR → `git revert <该 PR 提交>` 或从上一批提交点重建分支。

---

## 五、回退对照

| 现象 | 对应改动 | 回退方式 |
|---|---|---|
| 释卦页 rw7/rw8 失效 | PR #1（BUG-1） | `git revert 260808-fix-bug1-jiegua-comma` 合入点 |
| 历史页脚本执行/样式破坏 | PR #2（SEC-1） | revert PR #2 中 history.html 部分 |
| /api/ai 正常来源被拦 | PR #2（SEC-2 白名单） | 检查白名单配置，或 revert |
| 释卦失败提示又出现 config.js | PR #3（LOG-1） | revert |
| 历史重复膨胀 | PR #3（LOG-2） | revert |
| 角标/飞伏文案变化 | PR #5（ARC-2~5） | revert PR #5 |
