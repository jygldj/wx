# 增删卜易子系统 · 后续完善方案

> 撰写日期：2026-08-08
> 适用对象：`jygldj/wx` 仓库 `zsby/` 子系统（六爻占卜系统）
> 前置：前序《增删卜易子系统完善修正方案》已全部完成（5 个 PR 合入 main）。本文档承接其暂缓项（ARC-7/ARC-9/大重构）并新增移动端体验方向。
> 原则：**改动须慎重、分批实施、每批独立可验证可回退**；不触碰技术红线（suanfa.js 排盘核心、zhukong.js 主控流程、lunar.js、qwen.js/qwen2.js 提示词）。

---

## 〇、背景与范围

前序方案已完成缺陷修复（BUG-1/SEC-1/2/LOG-1/2）、架构收敛（ARC-1~5）、工程保障（ARC-6 单测、ARC-8 文档），全部落位 main 并通过回归验证。遗留三件暂缓项因"改动面大、风险高"未实施，本次重新评估并给出分步实施计划；另新增移动端体验方向（用户指定）。

### 本方案覆盖方向

| 方向 | 内容 | 上轮处置 | 本轮姿态 |
|---|---|---|---|
| ARC-7 | localStorage schema 版本化与字段统一 | 暂缓（涉及多页协议+旧数据迁移） | 评估 + 分步实施 |
| ARC-9 | 内联样式与 `yangshi.css` 合并 | 暂缓（纯视觉，无功能收益） | 评估 + 分步实施 |
| 大重构 | wnl.html/jiegua.html 内联 JS 迁入 `ly/` | 暂缓（高风险大改） | 评估 + 分步实施 |
| 移动端 | 移动端体验优化 | 新方向 | 评估 + 分步实施 |

### 红线（只读不改）

- `ly/suanfa.js` 排盘核心算法、`ly/zhukong.js` 主控流程、`lunar.js`、`api/qwen.js` 提示词。

---

## 一、现状侦查结果（2026-08-08 实测）

### 1.1 内联 JS 规模

| 页面 | 内联脚本字节 | 顶层函数数 |
|---|---|---|
| wnl.html | ~63 KB | ~40（equationOfTime/trueSolarTime/renderCalendar/renderHuangli/scanZeji 等） |
| jiegua.html | ~41 KB | ~15（getTimeInfo/fuShenRelation/mdToHtml/loadGuaData/displayGuaInfo/inferQuestionType 等） |
| jiegou.html | ~14 KB | — |
| history.html | ~5 KB | — |
| help.html | 0 | — |

### 1.2 localStorage 键与读写方（无版本号、多键共享）

| 键 | 写入方 | 结构要点 |
|---|---|---|
| `currentGua` | zhukong.js:349、jiegou.html:191、jiegua.html:1106 | guaData 对象：benGuaName/benPalace/bianGuaName/bianPalace/dongYao/yaoResults/dongStatus/benYao/bianYao/shiYao/yingYao/dongDetail/benFuShen/benMissingLiuQin/time |
| `gua_<id>` | zhukong.js:528、jiegou.html:307 | 暂存当前卦，jiegua.html:777-779 还原后即删（防膨胀） |
| `guaHistory` | zhukong.js:370-394、jiegua.html:1237-1249、history.html:290/368/390 | 数组：{benGua,bianGua,dongYao,question,time,userInfo,timeInfo,result,modelName} |
| `userInfo` | zhukong.js:365、jiegua.html:1419 | {name,gender,birth,question} |
| `wnlResult` | wnl.html:973 | 万年历结果缓存 |
| `tstj_timeInfo` | wnl.html:1509、jiegou.html:326-383 | 择吉时间信息（jiegou 读取后删除） |
| `tstj_zeji` | jiegua.html:1395、wnl.html:1660-1694 | 择吉数据（wnl 读取后删除） |

**问题**：无 schema 版本；三形态兼容代码量大（jiegua.html 内 `benYao/yaoDetail/guaInfo` 相关兼容代码约 53 处）；`gua_<id>` 临时键与 `tstj_*` 跨页传递键靠"用后即删"约定，无统一协议。

### 1.3 内联样式双轨

- JS 拼内联 style：jiegua.html 35 处、wnl.html 47 处、jiegou.html 13 处。
- 典型：`buildMarksHtml`（jiegua.html:857）用 `tagStyle` 字符串拼标签样式；jiegua.html:1199 择吉按钮内联 style + onmouseover/onmouseout 直接改背景色。
- 与 `ly/yangshi.css` 并存，改版需双处维护。

### 1.4 移动端现状

- viewport：五页均有；wnl.html 带 `maximum-scale=1.0, user-scalable=no`（禁用缩放，无障碍问题）。
- media query：jiegua 2 处、wnl 4 处（400/500/900px）、jiegou 0 处、history/help 各 2 处。覆盖不均。
- 事件绑定：addEventListener 为主（wnl 计时器/日历单元格），jiegua 少量内联 onclick（copyResult/saveResult/择吉按钮）暴露全局。
- 择吉按钮等宽元素未针对窄屏优化；`user-scalable=no` 应移除。

---

## 二、方向评估

### 2.1 ARC-7 localStorage schema 版本化

**问题定性**：状态管理脆弱——无版本号、多键共享、三形态兼容、跨页协议靠"用后即删"。当前**功能正确**（各写入/读取方配对良好），风险在**长期演进**：任一键结构变更即需同步改多处读取方，且无迁移路径。

**评估**：数据量小、结构稳定、无并发。收益主要为**演进安全性**（未来加字段/改结构有迁移保障）与**代码可读性**（消灭兼容分支）。风险在迁移层本身。

**推荐**：**分层轻量实施**——引入 `schemaVersion` + 读兼容层，逐步收敛三形态为单形态。不引入 localStorage 第三方库（零依赖原则），用自研 ~80 行轻量封装。

### 2.2 ARC-9 内联样式合并 yangshi.css

**问题定性**：JS 拼串样式与 CSS 文件双轨。典型痛点：jiegua.html:1199 择吉按钮 8 个内联样式属性 + 2 个 onmouseover/onmouseout；`buildMarksHtml`/`buildYongShenHtml` 大量 style 串。

**评估**：纯视觉重构、零功能收益但显著降低改版成本；风险极低（CSS 与样式串映射一一对应，可逐步替换）。**最值得先做**（低风险、即刻收益）。

**推荐**：分 3 批将"高频/静态样式串"迁移为 CSS 类：①角标标签（月破/日破/暗动/真空/假空）→ 预置类；②按钮/徽章等静态样式 → 类；③动态颜色（仅随数据变化的极少场景）保留内联并注释说明。onmouseover/onmouseout 改 CSS `:hover`。

### 2.3 大重构：内联 JS 迁入 ly/

**问题定性**：wnl.html ~63KB、jiegua.html ~41KB 内联脚本，无模块边界、函数依赖全局 DOM 初始化、onclick 全局暴露。

**评估**：**收益高**（可维护性/可测试性/缓存）但**风险最高**——两页均为完整应用，脚本加载时序、DOM 就绪、全局依赖、服务端 worker（`functions/api/ai.js`）无关但内联中 `tryIp/tryGps/reverseGeocode` 等涉外部 API 调用链。

**推荐**：**不做整页一次性迁移**。分两个阶段、每阶段一个页面、每页再拆分：
- 阶段 A：wnl.html → `ly/wnl.js`（纯函数层先出：equationOfTime/trueSolarTime/onlyDigits/padField 等无 DOM 依赖函数 → 可单测；再迁 render 层，最后迁事件绑定）。
- 阶段 B：jiegua.html → `ly/jiegua.js`（mdToHtml/mdToPlain/copyResult/saveResult 等纯逻辑先出）。
- 每步保持 `node --check` + 页面功能回归。

### 2.4 移动端体验

**问题定性**：viewport 有、基础 media query 有，但覆盖不均（jiegou 0 处）、wnl 禁用缩放、择吉按钮等元素未窄屏适配、内联 onclick 无触摸优化。

**评估**：收益高（移动端是占卜工具主要使用场景）、风险低。但**依赖 ARC-9**——移动端样式调整若叠加在样式双轨上，会放大改动面。故**顺序上先 ARC-9 再移动端**。

**推荐**：①移除 wnl `user-scalable=no`；②补齐 jiegou 响应式；③择吉按钮/操作按钮窄屏全宽；④触摸目标 ≥44px；⑤深色主题列入后续（与 ARC-9 类收敛后更易做）。

---

## 三、实施计划（分 6 批，每批独立验证可回退）

> 依赖关系：批次 1（ARC-9）独立、批次 2（ARC-7）独立、批次 3-4（大重构）建议在 ARC-7 后（迁移时统一读新封装）、批次 5（移动端）依赖批次 1。
> 每批在独立分支实施、单测通过后合入 main。

### 批次 1 — ARC-9 内联样式收敛（低风险，先做）

| 步骤 | 文件 | 改动 |
|---|---|---|
| 1.1 | ly/yangshi.css | 新增语义类：`.yong-tag`（角标基础）、`.yong-tag--yuePo/riPo/anDong/zhenKong/jiaKong`（五色角标）、`.action-btn`、`.zeji-btn`（择吉按钮，含 `:hover`） |
| 1.2 | jiegua.html | `buildMarksHtml` 的 `tagStyle` 串改为 `className` 引用；PRIORITY 表内 bg/color/border 删除，改类名映射 |
| 1.3 | jiegua.html | `buildYongShenHtml` 同类处理；择吉按钮内联 style + onmouseover/onmouseout → `class="zeji-btn"` |
| 1.4 | jiegua.html | 其余静态内联 style 串（buildGuaHtml 之外、不随数据变化的）迁类；仅动态值（如数据驱动的颜色）保留内联并注释 `/* 动态色 */` |
| 1.5 | wnl.html | `renderHuangli`/`buildCalCell` 的静态样式串迁 `yangshi.css` 预置类 |
| 1.6 | jiegou.html | 13 处内联样式串中静态部分迁类 |

**验证（V1）**：三页在桌面+移动视口截图对比改前/改后无可见差异（重点：角标颜色/圆角/择吉按钮 hover 效果）；`node --check` 各内联脚本通过；单测仍 17/17。

### 批次 2 — ARC-7 localStorage schema 版本化

| 步骤 | 文件 | 改动 |
|---|---|---|
| 2.1 | 新增 `ly/store.js` | 轻量封装（~80 行，零依赖）：`store.get(key, fallback)`/`store.set(key, val)`/`store.remove(key)`，统一 try-catch + JSON 编解码；写入时附 `__v:1` |
| 2.2 | 新增 `ly/store.js` 迁移钩子 | 读取时检查 `__v`，`v<2` 走 `migrateLegacy()`（识别当前 benYao/yaoDetail/guaInfo 三形态并存情况，规整为单形态），`v===undefined` 视为旧数据 |
| 2.3 | zhukong.js | `currentGua`/`guaHistory`/`userInfo` 写入改 `store.set`（经 `guaData` 组装点 349/365/394 收敛） |
| 2.4 | jiegua.html | 读取点（789/943/1106/1237/1308/1342/1371/1419）改 `store.get`；`gua_<id>` 读写（777-779）改封装 |
| 2.5 | jiegou.html | `currentGua`/`gua_<id>`/`tstj_timeInfo` 读写改封装 |
| 2.6 | wnl.html | `wnlResult`/`tstj_timeInfo`/`tstj_zeji` 读写改封装 |
| 2.7 | history.html | `guaHistory` 读写（290/368/390）改封装 |
| 2.8 | 新增 `ly/store.test.js` | 单测：写读回环、JSON 损坏容错、`__v` 缺失走旧数据路径、`gua_<id>` 用后即删语义 |

**验证（V2）**：`cd zsby/ly && node --test` 全绿（17+新增）；浏览器实测：模拟旧数据（手写无 `__v` 的 currentGua/guaHistory）加载后页面正常渲染、无迁移报错；排盘→释卦→历史→择吉全链路通。

### 批次 3 — 大重构阶段 A：wnl.html 迁 `ly/wnl.js`

| 步骤 | 文件 | 改动 |
|---|---|---|
| 3.1 | 新增 `ly/wnl.js` | 迁出**纯函数层**：equationOfTime/trueSolarTime/lunarOf/daysInMonth/onlyDigits/padField/zodiacOfYearGanZhi/hourGanZhi/getXunKong/getYuePo/getRiChong/getYiMa/getLiuShen/getLiuQin/getNaYin/escapeHtml/arrDiffDaysHours/p2（无 DOM 依赖者） |
| 3.2 | wnl.html | 原内联中删除已迁函数定义，`<script src="ly/wnl.js">` 引入（置于 lunar.js/shuju.js/suanfa.js 之后） |
| 3.3 | 新增 `ly/wnl.test.js` | 对纯函数层单测（干支推算/旬空/六神/纳音等，可复用既有验证基线） |
| 3.4 | 暂不迁 | renderCalendar/renderHuangli/renderYingqiResults/事件绑定等 DOM 依赖函数**留在内联**，第二阶段处理 |

**验证（V3）**：`node --check ly/wnl.js` + 内联脚本通过；单测全绿；万年历页功能回归（日历渲染/吉凶标注/择吉扫描/时间填充）。

### 批次 4 — 大重构阶段 B：jiegua.html 迁 `ly/jiegua.js`

| 步骤 | 文件 | 改动 |
|---|---|---|
| 4.1 | 新增 `ly/jiegua.js` | 迁出纯逻辑：mdInline/mdToHtml/mdToPlain/copyResult/fallbackCopy/saveResult/inferQuestionType |
| 4.2 | jiegua.html | 内联中删除已迁函数；`<script src="ly/jiegua.js">` 引入；`inferQuestionType` 原调用点改经 `ly/suanfa.js` 单源（若尚未，则经共享函数） |
| 4.3 | 新增 `ly/jiegua.test.js` | 对 inferQuestionType/mdToHtml/mdToPlain 单测（含 BUG-1 防回归用例） |
| 4.4 | 暂不迁 | loadGuaData/displayGuaInfo/buildMarksHtml/buildYongShenHtml/getTimeInfo 等 DOM/事件密集函数留内联 |

**验证（V4）**：同上；释卦页全链路（加载→排盘→释卦→应期→择吉跳转）回归。

### 批次 5 — 移动端体验（依赖批次 1）

| 步骤 | 文件 | 改动 |
|---|---|---|
| 5.1 | wnl.html | 移除 viewport `maximum-scale=1.0, user-scalable=no` |
| 5.2 | jiegou.html | 新增 `@media (max-width:500px)`：输入控件全宽、按钮可点区域加大、页边距收窄 |
| 5.3 | jiegua.html | 择吉按钮/操作按钮窄屏全宽（依赖批次 1 的 `.zeji-btn` 类）；角标区自动换行 |
| 5.4 | wnl.html | 日历单元格窄屏字号/间距优化（现有 400/500px 断点补全） |
| 5.5 | 全页 | 触摸目标 ≥44×44px 抽查（按钮/单元格/导航链接）；`-webkit-tap-highlight-color` 统一样式 |
| 5.6 | 可选 | 深色主题：新增 `yangshi.css` 内 `[data-theme="dark"]` 变量覆盖，暂只做 CSS 变量化准备，不切换默认主题 |

**验证（V5）**：真实手机或 DevTools 移动模拟：五页浏览无横向滚动、可点区域达标、缩放可用；择吉跳转链路在窄屏可用。

### 批次 6 — 收尾

| 步骤 | 文件 | 改动 |
|---|---|---|
| 6.1 | zsby/README.md | 目录树补 `ly/store.js`/`ly/wnl.js`/`ly/jiegua.js`；`ly/__tests__` 说明更新；移动端说明 |
| 6.2 | 方案文档 | 本文档标记各批次完成状态 |

---

## 四、验证清单汇总（每批必过）

| 编号 | 验证动作 | 关联批次 |
|---|---|---|
| V1.1 | 桌面+移动视口改前/改后截图对比无差异（角标/按钮/择吉 hover） | 1 |
| V1.2 | 三页内联 `node --check` 通过；单测 17/17 | 1 |
| V2.1 | `node --test` 全绿（17+store 新增） | 2 |
| V2.2 | 模拟旧数据（无 `__v`）加载正常，无迁移报错 | 2 |
| V2.3 | 起卦→排盘→释卦→历史→择吉全链路通 | 2 |
| V3.1 | `node --check ly/wnl.js`；万年历页功能回归 | 3 |
| V4.1 | `node --check ly/jiegua.js`；释卦页全链路回归 | 4 |
| V5.1 | 五页移动模拟无横向滚动、触控目标达标、缩放可用 | 5 |
| V5.2 | 择吉跳转窄屏可用 | 5 |
| V6.1 | README 目录树与实际文件一致 | 6 |

---

## 五、回退策略

- 每批独立分支（分支名 `260808-refactor-<方向>`），逐批合入 main。
- 批次 1（样式）：视觉回归不符即 `git revert`，不影响功能。
- 批次 2（存储）：store.js 封装为增量替换，任一页异常可单独还原该页读取点。
- 批次 3/4（大重构）：**分步迁移**，每步删函数+引外部文件同提交；页面功能回归失败则回退该步提交。
- 批次 5（移动端）：纯 CSS/viewport 改动，回退零成本。

---

## 六、收益与风险评估

| 方向 | 收益 | 风险 | 建议优先级 |
|---|---|---|---|
| ARC-9 样式收敛 | 消除双轨维护，为移动端铺路 | 极低 | **第 1 批** |
| ARC-7 存储版本化 | 演进安全、消灭兼容分支 | 中（迁移层需测透） | 第 2 批 |
| 大重构 wnl → ly/wnl.js | 可测性/可维护性 | 中高（页面应用迁移） | 第 3-4 批 |
| 大重构 jiegua → ly/jiegua.js | 可测性/可维护性 | 中高 | 第 3-4 批 |
| 移动端体验 | 主场景体验提升 | 低 | 第 5 批 |

**总原则**：与上轮一致的"小步快跑、每步可回退"。ARC-9 与移动端先落地（低风险、立即可见收益），ARC-7 与内联迁出作为中风险项按批次推进，全程不触碰技术红线。

---

## 七、暂缓项（本方案不实施，单独立项）

| 项 | 内容 | 缓办理由 |
|---|---|---|
| 深色主题切换 | 默认启用暗色主题 | 需先完成 CSS 变量化（本方案仅做 5.6 准备），UI 走查量大，独立立项 |
| 结果导出/分享、历史统计 | 导出图片、历史趋势 | 非核心链路，需求待用户进一步明确 |
| 内联 JS 全量迁出（render/事件层） | wnl/jiegua 剩余 DOM 依赖函数 | 需配合模块边界设计（目前页面无模块体系），第二阶段独立评估 |

---

## 八、实施记录

> 本方案各批次完成后在此登记。

| 批次 | 分支 | 内容 | 状态 |
|---|---|---|---|
| 1 | — | ARC-9 内联样式收敛 | 未开始 |
| 2 | — | ARC-7 localStorage schema 版本化 | 未开始 |
| 3 | — | 大重构 A：wnl → ly/wnl.js | 未开始 |
| 4 | — | 大重构 B：jiegua → ly/jiegua.js | 未开始 |
| 5 | — | 移动端体验 | 未开始 |
| 6 | — | README + 文档收尾 | 未开始 |
